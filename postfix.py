import csv 

with open('calculate_me.csv', 'r') as f: 
    data = csv.reader(f)
    for row in data:

def 

# if s == "A" or s == "B" or s == "C":
# if s in ["A","B","C","D", "E"]:
# int(word)
# str(avg)
#"{}", format(avg)